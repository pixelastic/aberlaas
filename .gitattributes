# Note: This can't be a symlink to the template stored in ./lib/templates as git
# otherwise will get confused by an infinite recursion of .gitignore files, so
# it needs to be a copy.

# Yarn
/.yarn/**            linguist-vendored
/.yarn/releases/*    binary
/.yarn/plugins/**/*  binary
/.pnp.*              binary linguist-generated
